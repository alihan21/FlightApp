<Page
    x:Class="FlightApp.Frontend.Pages.PassengerPages.OrderHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlightApp.Frontend.Pages.PassengerPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FlightApp.Frontend.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Canvas>
        <TextBlock Canvas.Left="612" Canvas.Top="135" Text="Order history" TextWrapping="Wrap" FontSize="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <ListBox x:Name="lbOrderHistory" ItemsSource="{Binding}" Height="404" Width="460" Canvas.Left="46" Canvas.Top="302" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <ListBox x:Name="lbTest" ItemsSource="{Binding}" Height="358" Margin="0,0,0,0" Width="463" Canvas.Left="952" Canvas.Top="325">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding OrderId}"/>
                        <TextBlock Text="{Binding Food.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Canvas>
</Page>
